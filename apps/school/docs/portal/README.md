# Portal Dokumentasi School App

Buka `index.html` di browser. Tanpa build step, tanpa dependency, tanpa CDN.

Sumber konten = `apps/school/docs/*.md`. Edit file `.md` lalu regenerasi portal
(jangan edit HTML langsung). Tambah screenshot ke `screenshots/`.
