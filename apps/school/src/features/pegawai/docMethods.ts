// Moved to @sekolahpro/api-client (docMethods.ts) so every module can call
// whitelisted DocType instance methods. Kept as a re-export for existing
// pegawai imports.
export { runDocMethod, useDocMethod, type RunDocMethodArgs } from "@sekolahpro/api-client";
